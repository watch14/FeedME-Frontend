import { LineBreakAfterPeriodPipe } from './line-break-after-period.pipe';

describe('LineBreakAfterPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new LineBreakAfterPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
